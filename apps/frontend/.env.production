# Frontend Production Environment Configuration
# This file contains production-ready settings for the Booking Swap frontend

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Production API base URL - UPDATE WITH YOUR PRODUCTION DOMAIN
VITE_API_BASE_URL=https://your-production-domain.com/api

# =============================================================================
# WEBSOCKET CONFIGURATION (Socket.IO)
# =============================================================================
# Production WebSocket server URL - UPDATE WITH YOUR PRODUCTION DOMAIN
VITE_WS_URL=https://your-production-domain.com

# Connection retry configuration (production optimized)
VITE_WS_RECONNECT_ATTEMPTS=10
VITE_WS_RECONNECT_INTERVAL=5000
VITE_WS_CONNECTION_TIMEOUT=15000

# Heartbeat configuration for connection health (production optimized)
VITE_WS_HEARTBEAT_INTERVAL=30000
VITE_WS_HEARTBEAT_TIMEOUT=10000

# =============================================================================
# FALLBACK CONFIGURATION
# =============================================================================
# Enable fallback mechanisms when WebSocket fails
VITE_ENABLE_FALLBACK=true
VITE_FALLBACK_POLLING_INTERVAL=30000

# =============================================================================
# PRODUCTION SECURITY CONFIGURATION
# =============================================================================
# Disable debug mode in production
VITE_WS_DEBUG_MODE=false
# Production log level - only show errors
VITE_WS_LOG_LEVEL=error

# =============================================================================
# HEDERA MAINNET CONFIGURATION
# =============================================================================
# Production Hedera network configuration
VITE_HEDERA_NETWORK=mainnet
# Mainnet mirror node URL
VITE_HEDERA_MIRROR_NODE_URL=https://mainnet.mirrornode.hedera.com

# =============================================================================
# WALLET CONNECT PRODUCTION CONFIGURATION
# =============================================================================
# Production WalletConnect Project ID - UPDATE WITH YOUR PRODUCTION PROJECT ID
VITE_WALLET_CONNECT_PROJECT_ID=your_production_wallet_connect_project_id_here

# =============================================================================
# FEATURE FLAGS CONFIGURATION - PRODUCTION DEPLOYMENT
# =============================================================================
# These flags control the visibility of advanced features in the production UI
# RECOMMENDED INITIAL DEPLOYMENT: All flags set to 'false' for simplified UI

# Auction Mode Features
# Controls auction acceptance strategy and related UI elements
# PRODUCTION RECOMMENDATION: Start with 'false' for simplified user experience
VITE_ENABLE_AUCTION_MODE=false

# Cash Swap Features  
# Controls cash payment options in swap creation modal
# PRODUCTION RECOMMENDATION: Start with 'false' to focus on core booking exchange
VITE_ENABLE_CASH_SWAPS=false

# Cash Proposal Features
# Controls cash offer functionality in proposal modal
# PRODUCTION RECOMMENDATION: Start with 'false' to simplify proposal process
VITE_ENABLE_CASH_PROPOSALS=false

# =============================================================================
# PRODUCTION FEATURE FLAGS DEPLOYMENT STRATEGY
# =============================================================================
#
# PHASE 1 - INITIAL DEPLOYMENT (RECOMMENDED):
# Deploy with all advanced features hidden to provide clean, simple UX
# VITE_ENABLE_AUCTION_MODE=false
# VITE_ENABLE_CASH_SWAPS=false
# VITE_ENABLE_CASH_PROPOSALS=false
#
# PHASE 2 - GRADUAL ROLLOUT (AFTER INITIAL STABILITY):
# Enable cash features first as they are simpler for users to understand
# VITE_ENABLE_AUCTION_MODE=false
# VITE_ENABLE_CASH_SWAPS=true
# VITE_ENABLE_CASH_PROPOSALS=true
#
# PHASE 3 - FULL FEATURE SET (AFTER CASH FEATURES ARE STABLE):
# Enable all features for complete functionality
# VITE_ENABLE_AUCTION_MODE=true
# VITE_ENABLE_CASH_SWAPS=true
# VITE_ENABLE_CASH_PROPOSALS=true
#
# EMERGENCY ROLLBACK:
# To instantly hide problematic features, set any flag to 'false' and restart frontend
# No code deployment required - changes take effect immediately
#
# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================
#
# 1. EASY ROLLBACK MECHANISM:
#    - Change any feature flag from 'true' to 'false'
#    - Restart frontend service: docker-compose restart frontend
#    - Changes take effect immediately without code deployment
#
# 2. BACKEND COMPATIBILITY:
#    - Backend APIs remain fully functional regardless of frontend flags
#    - All auction and cash swap data continues to work normally
#    - No backend configuration changes needed
#
# 3. MONITORING:
#    - Monitor user adoption and feedback for each feature phase
#    - Track error rates and support tickets by feature area
#    - Use gradual rollout to minimize risk
#
# 4. VALIDATION:
#    - Test each configuration in staging before production
#    - Validate feature flags are correctly applied after deployment
#    - Ensure rollback procedures work as expected
#
# =============================================================================