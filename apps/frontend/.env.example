# Frontend Environment Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# WALLET CONNECT CONFIGURATION
# =============================================================================
# Project ID from WalletConnect Cloud (https://cloud.walletconnect.com/)
VITE_WALLET_CONNECT_PROJECT_ID=your_wallet_connect_project_id_here

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Base URL for backend API endpoints
VITE_API_BASE_URL=http://localhost:3001/api

# =============================================================================
# WEBSOCKET CONFIGURATION (Socket.IO)
# =============================================================================
# WebSocket server URL (usually same as backend server)
VITE_WS_URL=http://localhost:3001

# Connection retry configuration
VITE_WS_RECONNECT_ATTEMPTS=10
VITE_WS_RECONNECT_INTERVAL=5000
VITE_WS_CONNECTION_TIMEOUT=10000

# Heartbeat configuration for connection health
VITE_WS_HEARTBEAT_INTERVAL=30000
VITE_WS_HEARTBEAT_TIMEOUT=5000

# =============================================================================
# FALLBACK CONFIGURATION
# =============================================================================
# Enable fallback mechanisms when WebSocket fails
VITE_ENABLE_FALLBACK=true
VITE_FALLBACK_POLLING_INTERVAL=30000

# =============================================================================
# DEBUG CONFIGURATION
# =============================================================================
# WebSocket debugging (set to true for development)
VITE_WS_DEBUG_MODE=false
# Log levels: error, warn, info, debug
VITE_WS_LOG_LEVEL=warn

# =============================================================================
# HEDERA NETWORK CONFIGURATION
# =============================================================================
# Hedera network: testnet, mainnet, previewnet
VITE_HEDERA_NETWORK=testnet
# Mirror node URL for the selected network
VITE_HEDERA_MIRROR_NODE_URL=https://testnet.mirrornode.hedera.com

# =============================================================================
# FEATURE FLAGS CONFIGURATION
# =============================================================================
# These flags control the visibility of advanced features in the UI
# Set to 'true' to enable, 'false' to disable (default: false)

# Auction Mode Features
# Controls auction acceptance strategy and related UI elements
# When disabled: Only "First Match Acceptance" strategy is available
VITE_ENABLE_AUCTION_MODE=false

# Cash Swap Features  
# Controls cash payment options in swap creation modal
# When disabled: Only "Booking Exchange Only" payment type is available
VITE_ENABLE_CASH_SWAPS=false

# Cash Proposal Features
# Controls cash offer functionality in proposal modal
# When disabled: Users can only propose booking exchanges, not cash offers
VITE_ENABLE_CASH_PROPOSALS=false

# =============================================================================
# FEATURE FLAGS USAGE NOTES
# =============================================================================
# 
# The feature flags system allows you to temporarily hide complex features
# while preserving all backend functionality. This is useful for:
# 
# 1. Simplifying the user interface during initial rollout
# 2. A/B testing different feature sets
# 3. Gradual feature rollout to different user groups
# 4. Quick rollback of features without code deployment
#
# IMPORTANT: 
# - These flags only affect the frontend UI visibility
# - Backend functionality remains intact regardless of flag values
# - Existing auction and cash swap data continues to work normally
# - Features can be re-enabled by simply changing these values to 'true'
#
# ROLLBACK STRATEGY:
# To quickly restore all features, set all flags to 'true':
# VITE_ENABLE_AUCTION_MODE=true
# VITE_ENABLE_CASH_SWAPS=true  
# VITE_ENABLE_CASH_PROPOSALS=true
#
# =============================================================================